diff --git a/cloudbaseinit/conf/default.py b/cloudbaseinit/conf/default.py
index 6190960..5b93d31 100644
--- a/cloudbaseinit/conf/default.py
+++ b/cloudbaseinit/conf/default.py
@@ -219,6 +219,8 @@ class GlobalOptions(conf_base.Options):
                     '.multipartmixed.MultipartMixedPlugin',
                     'cloudbaseinit.plugins.common.userdataplugins.heat.'
                     'HeatPlugin',
+                    'cloudbaseinit.plugins.common.userdataplugins.includeurl.'
+                    'IncludeUrlPlugin',
                 ],
                 help='List of enabled userdata content plugins'),
             cfg.ListOpt(
diff --git a/cloudbaseinit/plugins/common/userdata.py b/cloudbaseinit/plugins/common/userdata.py
index be25174..bce7b49 100644
--- a/cloudbaseinit/plugins/common/userdata.py
+++ b/cloudbaseinit/plugins/common/userdata.py
@@ -201,6 +201,25 @@ class UserDataPlugin(base.BasePlugin):
             user_data_plugins = factory.load_plugins()
             cloud_config_plugin = user_data_plugins.get('text/cloud-config')
             ret_val = cloud_config_plugin.process_non_multipart(user_data)
+        elif user_data.startswith(b'#include'):
+            LOG.debug('Found include URL in userdata')
+            user_data_plugins = factory.load_plugins()
+            include_url_plugin = user_data_plugins.get('text/x-include-url')
+            if include_url_plugin:
+                # Create a mock part object for the include plugin
+                from collections import namedtuple
+                MockPart = namedtuple('MockPart', ['get_payload', 'get_filename'])
+                mock_part = MockPart(
+                    get_payload=lambda decode=False: user_data if not decode else user_data,
+                    get_filename=lambda: 'include-urls'
+                )
+                ret_val = include_url_plugin.process(mock_part)
+                # Extract return code from tuple if needed
+                if isinstance(ret_val, tuple):
+                    ret_val = ret_val[0]
+            else:
+                LOG.warning('Include URL plugin not available')
+                ret_val = 1
         elif user_data.strip().startswith(x509constants.PEM_HEADER.encode()):
             LOG.debug('Found X509 certificate in userdata')
         else: