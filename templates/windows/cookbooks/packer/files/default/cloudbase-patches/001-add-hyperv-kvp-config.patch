diff --git a/cloudbaseinit/conf/default.py b/cloudbaseinit/conf/default.py
index 6190960..0e21755 100644
--- a/cloudbaseinit/conf/default.py
+++ b/cloudbaseinit/conf/default.py
@@ -295,6 +295,15 @@ class GlobalOptions(conf_base.Options):
                 'user_password_length', default=20,
                 help='The length of the generated password for the user '
                      'defined by the `username` config option.'),
+            cfg.BoolOpt(
+                'hyperv_kvp_reporting', default=False,
+                help='Enable HyperV KVP reporting for provisioning status. '
+                     'When enabled, provisioning events are reported via '
+                     'the HyperV Key-Value Pair exchange mechanism.'),
+            cfg.StrOpt(
+                'hyperv_kvp_incarnation', default='0',
+                help='Incarnation number for HyperV KVP reporting, used to '
+                     'identify reporting sessions.'),
         ]
 
         self._cli_options = [
